{
  "name": "@org/redux-todos",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "NODE_ENV=production babel src -d dist",
    "cleanup": "rimraf dist",
    "prepublish": "yarn cleanup && yarn test && yarn build",
    "test": "NODE_ENV=test jest src"
  },
  "optionalDependencies": {
    "react-redux": "^5.0.0",
    "redux": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-jest": "^19.0.0",
    "babel-preset-react-app": "^2.2.0",
    "jest": "^19.0.2",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "prop-types": "^15.5.6"
  }
}
